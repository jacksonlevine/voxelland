[workspace]
members = [
    "lib",
    "binaries/server",
    "binaries/client"
]

[profile.release]
opt-level = 2  # Maximum optimization level
debug = true   # Includes debug information even in release builds

[profile.deploy]
inherits = "release"
lto = true
opt-level = 3
codegen-units = 1
